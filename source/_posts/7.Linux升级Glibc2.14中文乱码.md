---
title: Linux升级Glibc2.14中文乱码
date: 2018/02/01 10:28:00
updated: 2018/02/01 10:28:00
urlname: update_glibc_issue
tags: [Linux]
categories: 
- Linux
---

{% note info %}

`CenterOS`上默认的`Glibc`版本为`2.12.1`,由于我们的Android项目使用到了`JNI`,将项目构建部署在服务器上时会遇到问题`'GLIBC_2.14' not found`,那么就需要升级`Glibc`至`2.14`.升级的教程网上很多,可是却漏掉了一个步骤,导致后面中文乱码的问题.本篇会做一个全面的解说

{% endnote %}

<!-- more -->

#### 1. Glibc下载

首先从[Glibc Download](http://www.gnu.org/software/libc/)下载包,这里选择是[Glibc 2.14](http://ftp.gnu.org/gnu/glibc/glibc-2.14.tar.gz)

#### 2. 安装

上传至服务器,进入目录解压

```
tar zxvf glibc-2.14.tar.gz
```

进入解压后的目录

```
cd glibc-2.14
```

接着

```
mkdir /opt/build
cd build
../configure --prefix=/opt/glibc-2.14
make -j4
make install
```

若顺利的话,至此则安装成功了.

##### issue

**1. 在make install过程中出现错误:**

```
Can't open configuration file /opt/glibc-2.14/etc/ld.so.conf: No such file or directory
```

缺少了报错的编译文件`ld.so.conf`.通过find命令找到对应的文件配置

![](https://ws1.sinaimg.cn/large/006tNc79ly1fo0wcs8mt3j30ap00vt8t.jpg)

然后拷贝文件至对应文件目录,继续编译

```
cp /etc/ld.so.conf /opt/glibc-2.14/etc/ 
make install 
```

编译成功,接着定义下环境变量即可

#### 3. 补充

{% note warning %}

通常的教程至此就结束了.最初笔者参考网上的教程也是做到这里.当时跑程序也没问题,直到前段时间大楼停电,服务器重启后,通过服务器上`Jenkins`打包上传到蒲公英的中文信息全是乱码! 相当于服务器的编码文件丢失了,所有中文都会乱码.

是因为遗漏了`localedata`未编译,在服务器重启后,`Glibc2.14`找不到对应版本`2.14`的`glibc locale`文件.

{% endnote %}

![](https://halcyon-1258836598.cos.ap-guangzhou.myqcloud.com/blog/006tNc79ly1fo0rvnge0hj30es0363yp.jpg)

正确操作是,在`make`&`make install`完成后继续编译local包

```
make localedata/install-locales
```

编译需要一些时间,完成后重启服务器即可生效

```
shutdown -r now
```

---

1. 参考: [http://blog.csdn.net/clirus/article/details/62425498](http://blog.csdn.net/clirus/article/details/62425498)
2. 参考: [http://blog.csdn.net/fzuzhanghao1993/article/details/77650500](http://blog.csdn.net/fzuzhanghao1993/article/details/77650500)

