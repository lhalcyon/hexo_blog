---
title: TencentOS4安装Docker
date: 2025/04/18
updated: 2025/04/18
urlname: tencentos4-install-docker
tags: [Docker]
categories: 
- Docker
---

{% note info %}

记录下 TencentOS Server 4 安装 docker 和 docker compose

{% endnote %}

各种 Linux 安装 Docker 的方式略有区别 , 这里记录下 TencentOS Server4的方式

### 安装 Docker

该版本操作系统公共镜像已预置腾讯云的 docker 源，可执行以下命令，安装 Docker。

```
sudo yum install docker -y
```

执行以下命令，自启动和运行 Docker。

```
sudo systemctl enable docker
sudo systemctl start docker
```

执行以下命令，检查安装结果。

```
docker info
```

### 安装 Docker Compose

```
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose

sudo chmod +x /usr/local/bin/docker-compose
```



#### 参考链接

[腾讯云文档](https://cloud.tencent.com/document/product/213/46000)
